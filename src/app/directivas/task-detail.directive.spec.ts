import { TaskDetailDirective } from './task-detail.directive';

describe('TaskDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDetailDirective();
    expect(directive).toBeTruthy();
  });
});
